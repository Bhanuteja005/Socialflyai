// Type-only entry for the web app: `import type { AppType } from "@socialfly/api"`.
// Importing app.ts for its TYPE pulls no runtime code into the browser bundle.
export type { AppType } from "./app";
