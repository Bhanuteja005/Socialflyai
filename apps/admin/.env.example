# Browser-visible config — inlined at BUILD time (docker build args in CI).
NEXT_PUBLIC_API_URL=http://localhost:4400
NEXT_PUBLIC_AUTH_URL=http://localhost:4800
# The auth service's staff client: login only, origin http://localhost:4702.
NEXT_PUBLIC_AUTH_CLIENT_ID=socialfly-admin
# This console's own public URL (metadata base).
NEXT_PUBLIC_ADMIN_URL=http://localhost:4702
# The product app — the console links there for support context.
NEXT_PUBLIC_APP_URL=http://localhost:4700
NEXT_PUBLIC_SITE_URL=http://localhost:4701
NEXT_PUBLIC_SENTRY_DSN=
